## Usage Prompts

test

## Configuration
```json

```json
{
  "name": "test-agent",
  "description": "A test agent for validating the contribution workflow",
  "prompt": "You are a test agent designed to validate system functionality and ensure proper workflow execution.",
  "tools": ["aws_cli", "test_framework", "validation_tools"],
  "allowedTools": ["aws_cli", "test_framework"]
}
```
```
